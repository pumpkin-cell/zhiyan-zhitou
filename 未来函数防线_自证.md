# 未来函数泄露自证（Look-ahead Bias 防线）

> 核心结论：本项目的回测/复现代码**没有未来函数泄露**，由以下 5 道防线保证，且代码全部公开可逐行审计。

## 什么是未来函数泄露
在 t 时刻做决策时，错误使用了 t 之后（未来）才可得的信息，导致回测虚高、实盘失效。
这是量化投研最常见的"隐性造假"，也是评委必问的"可信"拷问点。

## 5 道防线（均可在代码中定位）

### 1. 信号滞后（核心防线）
持仓决策只使用 t 日及之前的收盘数据；t 日收盘后才调仓，新持仓 **t+1 日才生效**。
- `rotate_backtest_v4.py` → `run_strategy()` 循环：先按「旧持仓」结算 t 日收益，再据 t 日数据调仓
- `research_agent.py` → `ma_factor()`：`signal.shift(1)`（信号滞后一天）

### 2. 盘中情绪只用 15:00 前新闻
情绪指数只统计当日 **15:00 前**的新闻（盘中信息），杜绝用盘后新闻"预测"当日走势。
- `build_sentiment_indices.py`：`news['hour'] < 15`
- `build_market_sentiment.py`：`sub["datetime"].dt.hour < 15`

### 3. 训练集均值中性化
情绪因子的缺失值/中性化只用**前 70% 数据（训练集）**的均值，绝不用全样本（含未来）均值。
- `factor_cards.py`、`rotate_backtest_v4.py`：`iloc[:int(len(s) * 0.7)].mean()`

### 4. 训练/测试严格切分
样本外验证：前 70% 训练、后 30% 测试，测试集全程不参与任何参数选择。
- `sample_out_validation.py`

### 5. IC 评估与交易信号分离
因子 IC（预测力）用 `shift(-horizon)` 取未来收益计算，但**仅用于研究性评估预测力**，不进入交易信号。
- `reproduce_batch.py` → `ic_of()`：`fr = px.pct_change(horizon).shift(-horizon)`

## 如何自证（对外）
1. **代码公开**：全部策略/回测/因子代码放 GitHub public 仓库，任何人可逐行审计
2. **防线可定位**：上表每一道防线都标注了文件与代码行，可现场打开验证
3. **主动披露衰减**：样本外表现（训练 0.39 / 测试 0.40）主动披露轻微衰减，证明未过拟合、未用未来数据"美化"结果
