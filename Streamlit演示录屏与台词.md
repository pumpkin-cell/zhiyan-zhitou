# Streamlit 演示 · 录屏方案 + 配音台词脚本

> 目标：录一段 60~90 秒的演示视频，展示系统能"点开用"，录完自己配音/配字幕。

---

## 一、怎么录（超简单，三选一）

| 工具 | 优点 | 怎么用 |
|---|---|---|
| **Xbox Game Bar**（推荐） | 系统自带、零安装 | 按 `Win + G` → 点「捕获」→ 圆点开始录制 → 录完再按 `Win + G` 停止 |
| **剪映** | 录完直接配字幕 | 剪映 → 「录屏」→ 录完拖进时间轴配字幕 |
| **OBS Studio** | 专业、清晰 | 新建「显示器采集」→ 开始录制 |

> 录制时务必**全屏浏览器**、把命令行窗口最小化，画面干净。

---

## 二、录前准备（务必先做）

1. **设 API Key**（③ 智能体必须有，否则录不了结果）：
   ```powershell
   $env:DEEPSEEK_API_KEY="你的key"
   streamlit run app.py
   ```
2. **自己先跑一遍**，熟悉三个按钮位置，录的时候不卡壳。
3. **浏览器只开 Streamlit 一个页面**，缩放调好，字号够大。

---

## 三、录屏操作顺序（照着点）

```
① 打开 Streamlit → 首页停在「① 因子复现」
   └─ 选「反转」因子 → 点「🚀 运行复现」→ 等净值曲线出来

② 左侧切「② 因子卡片评估」
   └─ 点「📊 生成因子卡片」→ 等 IC 表出来

③ 左侧切「③ 自然语言智能体」
   └─ 输入框敲「复现20日动量因子，在沪深300ETF上」
   └─ 点「🤖 运行智能体」→ 等三步结果（意图→复现→报告）
```

---

## 四、配音台词脚本（照着念，配字幕即可）

**【00:00 - 00:08】开场**
"这是我们团队做的量化投研智能体系统。整个系统跑在本地，打开浏览器就能用。"

**【00:08 - 00:28】① 因子复现**
"先看第一个功能——因子复现。我选一个反转因子，回看窗口二十个交易日，点运行。系统自动调用确定性的回测引擎，几秒钟跑出一条净值曲线，同时给出年化收益、夏普比率、最大回撤、日胜率四个指标。"

**【00:28 - 00:48】② 因子卡片**
"第二个功能——因子卡片。点一下生成，系统自动对每个因子算 IC 值。IC 绝对值越大、预测力越强；正负号表示方向——红色的负值说明它是反向指标。比如情绪因子：新闻情绪越高涨，未来收益反而越低。"

**【00:48 - 01:18】③ 自然语言智能体（核心）**
"第三个，也是最核心的——自然语言智能体。我不写一行代码，只打一句话：复现二十日动量因子。点运行。第一步，它自动理解了我的意图，识别出因子、窗口和标的。第二步，它自动跑了复现。第三步，它自动生成了一份研究报告。一句话，从理解、到复现、到报告，全自动完成。"

**【01:18 - 01:28】收尾**
"这就是我们的系统——把量化研究员从重复劳动里解放出来，把时间还给创造性的判断。"

---

## 五、两个提醒

1. **③ 智能体如果没有 API Key**：这段就只能录到"输入一句话"的画面，录不出结果。如果 key 没准备好，就跳过 ③，只录 ① ② 两段，时长缩短到 50 秒，也够用。
2. **配音可以事后配**：录的时候不用说话，录完拖进剪映，照着上面的台词配字幕/录音，最省事。
